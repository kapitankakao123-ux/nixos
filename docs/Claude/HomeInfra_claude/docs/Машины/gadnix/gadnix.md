---
tags: [хост, gadnix]
---

# gadnix — домашний ПК

x86_64. Раньше проходил как `desktop` (placeholder), имя выбрано: **gadnix**. Сейчас Arch (полтора года). Мигрирует на NixOS последним — когда схема обкатана на [[kitjet]] и [[NixOS-на-Pi5]].

## Роли

- **игры** — Steam, Proton
- **браузер**
- **работа** — RDP, WinBox, LibreOffice, VS Code
- **оболочка** — niri + DMS, конфиг общий с [[kitjet]]

Виртуализации нет, хранилища нет.

## Отличия от kitjet

- виртуалок нет → `modules/virtualisation.nix` не импортируется
- хранилища нет → `modules/storage.nix` не импортируется
- зато есть Steam, Sunshine, VIA/QMK
- графическая часть общая: `modules/desktop.nix` + `home/common.nix` дают то же окружение niri + DMS

## Рабочий софт: чем закрывается

| Задача | Пакет в nixpkgs | Заметка |
| --- | --- | --- |
| RDP-клиент | `remmina` / `freerdp` | freerdp3 — голый CLI, remmina — GUI поверх него |
| WinBox | `winbox` (или `winbox4`) | старый — Windows-бинарь под wine, тянет wine целиком. `winbox4` нативный, если версия устройств позволяет |
| Офис | `libreoffice-fresh` | |
| Редактор | `vscode` / `vscodium` | `vscode` несвободный, нужен `allowUnfree`. Расширения ставятся либо через маркетплейс в `~`, либо декларативно через `vscode-with-extensions` |
| Игры | `programs.steam.enable` | системная опция, не home — тянет 32-битные библиотеки и firewall-правила |

- [ ] Проверить, какая версия WinBox нужна под текущие RouterOS
- [ ] Решить по VS Code: расширения декларативно или императивно (декларативно чище, но обновлять руками)

## Про «общий конфиг niri» — как это работает на самом деле

Решено: `~/.config/niri/config.kdl` **остаётся обычным файлом**, не под home-manager (правки применяются мгновенно, `niri validate` работает, пересборка не нужна).

Следствие, о котором надо помнить: **«общий конфиг» с kitjet сейчас поддерживается вручную.** Автоматической синхронизации нет, файлы разъедутся, если за этим не следить.

Второе: даже общий конфиг не может быть *идентичным*. kitjet — телевизор (смотришь с дивана: другой `scale`, крупнее шрифты, другие имена выходов), gadnix — монитор на столе. Общей может быть логика (биндинги, правила окон, вид), различаться будут блоки `output`.

> **Вариант на будущее, который снимает этот компромисс:** `config.lib.file.mkOutOfStoreSymlink`. Home-manager делает симлинк на **изменяемый** файл в репозитории, а не копию в `/nix/store`. Файл остаётся редактируемым, правки мгновенные, `niri validate` работает — и при этом он в git и общий для машин. Вернуться, когда конфиг устаканится.

## Подготовка (пока Arch жив)

- [ ] Выписать список установленных пакетов: `pacman -Qqe > ~/arch-packages.txt`
- [ ] Найти для каждого аналог в nixpkgs (`nix search nixpkgs <имя>`), отметить чего нет
- [ ] Отдельно: Zen Browser — только через flake, в nixpkgs его нет (input уже есть во `flake.nix`)
- [ ] Забрать дотфайлы, которые ещё не в репозитории
- [ ] Сохранить конфиг niri из `~/.config/niri/`
- [ ] Скопировать ssh-ключи и `~/.gnupg`
- [ ] Бэкап домашней папки на `tank` кухонного сервера
- [ ] Проверить, что список игр/данных Steam не потеряется (или готовность перекачать)

## Установка

- [ ] Разметка (если нужен ZFS/btrfs на корне — решить заранее, потом переделывать больно)
- [ ] Установить NixOS с минимальной конфигурацией
- [ ] `nixos-generate-config` → положить `hardware-configuration.nix` в `hosts/gadnix/`
- [ ] Вписать `gadnix` во `flake.nix` и в `networking.hostName`
- [ ] Задать `networking.hostId` — свой, отличный от kitjet и rpinix
- [ ] Проверить видеодрайверы (NVIDIA — отдельная история с `hardware.nvidia`)
- [ ] `colmena apply-local` или `sudo nixos-rebuild switch --flake ~/nixos`

## После

- [ ] Разложить недостающий софт по `home/gadnix.nix`
- [ ] Проверить Steam и Proton
- [ ] Проверить VIA — udev-правила приходят из `hardware.keyboard.qmk.enable`
- [ ] Sunshine: раскомментировать и проверить стриминг
- [ ] Убедиться, что окружение niri/DMS идентично кухонному — если нет, разница уехала не в тот модуль

[[00-Проект]] · [[Репозиторий]] · [[kitjet]]
